#include<graphics.h>
#include <stdio.h>
int main()
{

 void circle( int x, int y, int radius );
 int getdisplaycolor( int color );
 int converttorgb( int color );
 void delay( int msec );
 int getch( );
 int kbhit( );
}
